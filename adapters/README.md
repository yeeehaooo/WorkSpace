# adapters

## Responsibility
- Tool adapter specifications and mappings.
- Defines how governance/skills are loaded and represented in each tool (Cursor, Claude, etc.).

## What this folder must NOT contain
- The source-of-truth governance content (belongs to /agent).
- Reusable skill implementations (belongs to /skills).
- Any project-specific rules (belongs to the project repository).
